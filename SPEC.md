# 实验室组会记录网站 - 规格说明书

## 1. 概念与愿景

一个温暖、专业的实验室组会记录平台，帮助团队记录每次组会的文献分享、文字稿和 PPT，让组会内容井然有序、随时可查。设计上追求学术感与清新感的平衡——既专业可信，又不沉闷乏味，像一本精心装订的组会手记。

## 2. 设计语言

**美学方向**：学术期刊 + 现代笔记工具。干净的纸张质感，柔和的学术配色，参考 Notion 的极简排版与文献管理工具的严谨感。

**色彩方案**：
- 主色：`#2C5282`（深蓝，学术沉稳感）
- 次色：`#EBF4FF`（浅蓝，背景与卡片）
- 强调色：`#DD6B20`（暖橙，用于标签与高亮）
- 背景色：`#F7FAFC`
- 文字主色：`#1A202C`
- 文字次色：`#718096`
- 边框色：`#E2E8F0`
- 成功色：`#38A169`
- 危险色：`#E53E3E`

**字体**：
- 标题：`"Noto Serif SC", Georgia, serif`（正文宋体，学术感）
- 正文/UI：`"Inter", "Noto Sans SC", system-ui, sans-serif`（无衬线，清晰易读）

**空间系统**：基于 8px 网格。内边距 16/24/32px，卡片间距 16px，大区块间距 48px。

**动效哲学**：微妙的过渡动画。页面切换淡入淡出（200ms ease），卡片悬停轻微上浮（translateY -2px, box-shadow 加深），模态框从底部滑入（300ms ease-out）。不强硬，留有余韵。

**视觉资产**：
- 图标：Lucide Icons（通过 CDN 内联 SVG）
- 无装饰性图片，以排版和留白取胜
- 卡片圆角 8px，微妙阴影

## 3. 布局与结构

**三页面架构**（纯前端 SPA，通过 hash 路由切换）：

```
┌──────────────────────────────────────────────┐
│  顶部导航栏（网站名 + 按钮）                     │
├──────────────────────────────────────────────┤
│  [页面1] 首页：                                 │
│  ┌──────────────┐  ┌───────────────────────┐   │
│  │ 组会记录列表   │  │ 文献资料库              │  │
│  │ (按时间倒序)   │  │ (所有文献汇总+搜索)      │  │
│  └──────────────┘  └───────────────────────┘   │
├──────────────────────────────────────────────┤
│  [页面2] 添加/编辑组会页面                       │
│  表单：时间 + 参与者 + 文献条目(可多添加)         │
├──────────────────────────────────────────────┤
│  [页面3] 组会详情页面                           │
│  组会信息 + 参与者卡片(含文字稿+PPT链接)         │
└──────────────────────────────────────────────┘
```

**响应式策略**：桌面端双栏布局（列表+文献库并排），移动端单栏堆叠。

## 4. 功能与交互

### 核心功能

**组会记录管理**：
- 添加新组会：填写日期时间、参与者名单
- 为每个参与者添加文献（文献名、作者/期刊、DOI/链接、文字稿内容）
- 为每份文献上传/关联 PPT 文件（支持南大云盘、GitHub 远程或本地 Base64 三种存储方式）
- 编辑已有组会
- 删除组会（二次确认）

**文献资料库**：
- 展示所有组会中的文献列表
- 按文献名或作者搜索
- 按关键词筛选
- 点击直接跳转到对应组会详情页

**数据存储**：
- 组会元数据存储在浏览器 localStorage 中
- PPT/PDF 文件支持三种存储方式（按优先级自动选择）：
  1. **南大云盘**（推荐）：通过 WebDAV 直传 box.nju.edu.cn，适合大文件和实验室内部长期存储
  2. **GitHub**：通过 Vercel API 代理存储到 GitHub 仓库 `uploads/` 目录
  3. **本地**：Base64 编码存储在 localStorage，适合小文件（≤5MB）
- 数据格式：JSON 数组
- 支持数据导出为 JSON 文件（备份）
- 支持从 JSON 文件导入（恢复）

### 交互细节

- 模态框用于添加/编辑表单，阻止背景滚动
- 文献条目可动态增删（"添加文献"按钮，点击 X 删除）
- 每份文献的 PPT 支持拖拽上传或点击选择文件
- 文字稿为多行文本框，支持手动粘贴/输入
- 删除按钮悬停变红，确认对话框防止误操作
- 搜索框实时过滤（300ms 防抖）

### 边界条件

- 空状态：首次使用时显示引导提示（"还没有组会记录，点击上方按钮开始记录第一次组会吧"）
- 搜索无结果：显示友好提示
- localStorage 接近满时（>4MB），提示用户导出备份

## 5. 组件清单

### 导航栏
- 左侧：网站名称（带书本图标）
- 右侧："新建组会"按钮
- 固定顶部，带底部阴影

### 组会卡片
- 显示：日期、参与者数量、文献数量
- 悬停：背景变亮，阴影加深
- 点击：跳转到组会详情

### 文献库搜索栏
- 带搜索图标的输入框
- placeholder："搜索文献名或作者..."
- 实时过滤

### 文献条目卡片
- 显示：文献标题（加粗）、作者/期刊、关键词标签
- 显示关联的组会日期（点击跳转）
- 若有 PPT 图标则显示

### 表单模态框
- 半透明深色遮罩（rgba(0,0,0,0.5)）
- 白色内容框，圆角 12px
- 头部：标题 + 关闭按钮
- 底部：取消 + 确认按钮

### 空状态
- 居中图标 + 文字提示
- 引导性文案

## 6. 技术方案

**技术栈**：
- 纯 HTML5 + CSS3 + Vanilla JavaScript（ES6+）
- 无框架依赖，单文件即可运行
- 通过 hash 路由（`#meeting-list`、`#new-meeting`、`#meeting/:id`）实现 SPA 导航

**数据模型**：

```javascript
// 组会记录
{
  id: "uuid-string",
  date: "2026-03-29T10:00",       // ISO 格式日期时间
  topic: "第12次组会",              // 可选主题名称
  participants: [
    {
      id: "uuid-string",
      name: "张三",
      literature: [
        {
          id: "uuid-string",
          title: "文献标题",
          authors: "作者信息",
          journal: "期刊名称",
          doi: "10.xxxx/xxxx",
          link: "https://...",
          pptFileName: "xxx.pdf",  // 文件名
          pptDataUrl: "data:...",  // Base64 编码的 PPT
          transcript: "文字稿内容...", // 纯文本
          keywords: ["关键词1", "关键词2"]
        }
      ]
    }
  ],
  notes: "组会备注...",
  createdAt: "2026-03-29T10:00:00.000Z",
  updatedAt: "2026-03-29T10:00:00.000Z"
}
```

**文件结构**：

```
实验室组会记录网站/
├── index.html          # 主页面（包含所有 HTML 结构和 JS/CSS）
├── SPEC.md             # 本规格说明书
└── README.md           # 使用说明
```

**PPT 存储策略**：
- 使用 FileReader API 将用户上传的 PPT/PDF/图片文件转为 Base64 data URL
- 存储在 localStorage 中（适用于小型文件）
- 提醒用户：大型 PPT 建议压缩或转换为 PDF 以节省空间

**搜索与过滤**：
- 客户端实时过滤，300ms 防抖
- 匹配字段：文献标题、作者、期刊、关键词
